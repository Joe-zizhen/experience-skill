interface:
  display_name: "Systematic Debugging"
  short_description: "Reproduce the failure, find the root cause, then fix it"
  default_prompt: "Use $systematic-debugging to reproduce this bug, find its root cause, and fix it (failing test first, one change at a time)."

policy:
  allow_implicit_invocation: true
